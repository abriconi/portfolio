import { HeadingDetailed } from "../shared-components/HeadingDetailed/HeadingDetailed";
import styles from "./styles.module.scss";

export const Features = () => {
  return (
    <div className={styles.wrapper}>
      <HeadingDetailed text="Features" />
    </div>
  );
};
